# mq-demo



test



ddf

## Test Setup

- Create and activate a Python virtual environment: `python3 -m venv venv && source venv/bin/activate`
- Install the test dependency: `pip install pytest`
- Run the tests: `pytest test_math_operations.py -v`
